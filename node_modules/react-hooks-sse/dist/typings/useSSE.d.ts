import { Event } from './source';
import { SSEContext } from './SSEContext';
declare type Action<T> = {
    event: Event;
    data: T;
};
declare type StateReducer<S, T> = (state: S, changes: Action<T>) => S;
declare type Parser<T> = (data: any) => T;
export declare type Options<S, T = S> = {
    stateReducer?: StateReducer<S, T>;
    parser?: Parser<T>;
    context?: typeof SSEContext;
};
export declare function useSSE<S, T = S>(eventName: string, initialState: S, options?: Options<S, T>): S;
export {};
